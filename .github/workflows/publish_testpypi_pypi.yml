name: Publish to PyPI and TestPyPI

on:
  release:
    types:
      - created

jobs:
  package-build-and-publish:
    name: Build and publish package to TestPyPI and then to PyPI
    runs-on: ubuntu-18.04

    steps:
      - uses: actions/checkout@master
      - name: Set up Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade setuptools wheel

      - name: Build package
        run:
          python setup.py sdist bdist_wheel

      - name: Only do this when tagged
        run: echo "Hello!!! github.ref - ${{ github.ref }}; github.event.ref - ${{ github.event.ref }}; github.event_name - ${{ github.event_name }}"

      - name: Publish package to TestPyPI
        if: startsWith(github.event.ref, 'refs/tags') && github.event_name == 'release'
        uses: pypa/gh-action-pypi-publish@master
        with:
          password: ${{ secrets.test_pypi_password }}
          repository_url: https://test.pypi.org/legacy/

      - name: Publish package to PyPI
        if: startsWith(github.event.ref, 'refs/tags') && github.event_name == 'release'
        uses: pypa/gh-action-pypi-publish@master
        with:
          password: ${{ secrets.pypi_password }}
